/**
 * This package contains the core domain objects that are used to represent the
 * application's request/response {@code data transformation objects(DTOs)}.
 *
 * <p>
 * These models map directly to the API parameters and are used to map the
 * request, and form response and error responses shared by the application.
 * </p>
 *
 * <h2>Components</h2>
 * <ul>
 * <li>{@link com.utilities.log.Encryption} - Represents a request and
 * response data transformation object (DTO) in the system.</li>
 * <li>{@link com.utilities.log.AppError} - Represents a error response
 * data transformation object (DTO) in the system.</li>
 * <li>{@link com.utilities.log.UserDto} - Represents a User request and
 * response data transformation object (DTO) in the system.</li>
 * </ul>
 * 
 * 
 * @author Kabir Akware
 */

package com.utilities.log;